upstream backend {
    server app-blue:3000;
    # server app-green:3000;   <-- swap the comment to flip
}

server {
    listen 80;
    location / {
        proxy_pass http://backend;
    }
    location /health {
        proxy_pass http://backend/health;
    }
}
